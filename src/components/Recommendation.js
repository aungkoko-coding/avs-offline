import React from 'react';
import { Box, Typography } from '@mui/material';

export default function Recommendation({ currentGenres }) {
    return (
        <Box>
            <Typography>
                Recommendations
            </Typography>
        </Box>
    )
}